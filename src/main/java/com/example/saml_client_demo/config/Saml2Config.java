package com.example.saml_client_demo.config;

// Temporarily disable custom SAML configuration to test auto-configuration
// The issue appears to be with signature validation

public class Saml2Config {
    // Configuration will be handled by Spring Boot auto-configuration
    // based on application.properties or metadata file
}